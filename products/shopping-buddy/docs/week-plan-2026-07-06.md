# Week plan — 6–10 July 2026

Local demo only. SQL Express on `DESKTOP-J5AN1SG\SQLEXPRESS`. Dapper behind repository interfaces.

---

## Day 1 — Sunday 6 July (today)

**Goal:** Database live + API connected on your laptop.

| Task | You | Status |
|------|-----|--------|
| Pull PR #2 / latest branch | `git pull` | |
| Enable Mixed Mode (optional today) | SQL Server Configuration Manager | |
| Run `database\run-all.bat` | SSMS verify 5 users, 5 buddies | |
| `dotnet run` in AuthApi | Swagger opens | |
| `ng serve --port 4300` | Login as customer@demo.com | |
| LinkedIn post (portfolio version) | Option A from earlier chat | |

**Deliverable:** Login works against SQL Express; Find buddy shows DB data.

---

## Day 2 — Monday 7 July

**Goal:** End-to-end booking saved in SQL.

| Task | Detail |
|------|--------|
| Book Meera K. at Phoenix Wakad | Full form + safety checkbox |
| My bookings page | See BMSB-100001 seed + your new booking |
| Swagger test | `POST /api/bookings` with JWT |
| SQL proof | `SELECT * FROM dbo.Bookings` in SSMS |

**Learning:** Trace `IBookingRepository` → `BookingRepository` → INSERT script mentally.

**Deliverable:** One new row in `dbo.Bookings` you created from the UI.

---

## Day 3 — Tuesday 8 July

**Goal:** SQL auth users + polish errors.

| Task | Detail |
|------|--------|
| Enable Mixed Mode + run `004_sql_logins.sql` | Test `sb_app_ro` / `sb_app_rw` in SSMS |
| Switch API to `sb_app_rw` connection (optional test) | Change active connection string once |
| Loading / error messages | API down, wrong password, empty search |
| Screenshot | Find buddy with 3+ cards |

**Deliverable:** Read-only login cannot INSERT; rw login can.

---

## Day 4 — Wednesday 9 July

**Goal:** Admin view + buddy card UI.

| Task | Detail |
|------|--------|
| Admin: all bookings list (simple table) | `GET /api/bookings` for Admin role |
| Buddy avatars on find + book pages | Already seeded URLs |
| Hide any leftover legacy UI noise | Quick nav check |
| README + 5-min screen recording | Loom or phone |

**Deliverable:** Admin sees all bookings; demo video recorded.

---

## Day 5 — Thursday 10 July

**Goal:** Buffer + Week 2 prep.

| Task | Detail |
|------|--------|
| Retest full flow on clean browser | Incognito |
| Git commit + push your local fixes | |
| Note blockers for Week 2 | Buddy dashboard, LinkedIn project entry |
| Optional: SQL script `005_` for your new columns | Only if you have design ideas |

**Deliverable:** Written list of what you want in Week 2 (send to me).

---

## Daily time budget

| Activity | Hours |
|----------|-------|
| Hands-on coding / testing | 2–3 |
| SQL + SSMS exploration | 0.5–1 |
| Docs / LinkedIn / video | 0.5–1 |
| **Total** | **~3–4 h/day** |

---

## Architecture this week (unchanged)

```
SQL scripts (source of truth)
    ↓
Dapper repositories (IUserRepository, IBuddyRepository, …)
    ↓
ASP.NET Core 8 controllers
    ↓
Angular 15 (localhost:4300)
```

EF Core: not this week. Scaffold from DB in Week 5+ if you want LINQ practice.

---

## Azure DB

**Not this week.** Stay on SQLEXPRESS. Azure only when you explicitly opt in for a public demo.
